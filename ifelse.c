#include<stdio.h>
int main()
{
   int number,rem;
   printf("\nEnter the number to be checked odd or even");
   scanf("%d",&number);
   rem=number%2;
   if(a>b)
   {
       printf("\n%d is even",number);
   }
   else
   {
       printf("\n%d is odd",number);
   
   }
   return 0;
}